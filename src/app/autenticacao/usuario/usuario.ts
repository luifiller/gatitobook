export interface Usuario {
  // o uso de '?' antes dos dois pontos sinaliza que o retorno de uma resposta é opcional, ou seja, pode ser null/undefined
  id?: number;
  name?: string;
  email?: string;
}
